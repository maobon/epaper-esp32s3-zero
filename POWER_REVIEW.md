# 功耗检查与修改

检查范围：项目全部应用源码、配置、现有构建参数，以及本机 Arduino-ESP32 3.3.11 / GxEPD2 1.6.9 的相关驱动实现。未读取或修改 Secrets.h。

## 已修正

| 原行为 | 修改后 |
| --- | --- |
| Wi-Fi 在下载后保持连接，并自动重连 | 校时、批量下载结束（包括失败）后关闭无线；下一次请求再连接 |
| loop 每 10 ms 运行，未使用芯片休眠 | 计算最近的翻页、传感器、校时重试、下载重试截止时间，定时浅睡眠 |
| 屏幕初始化后等待下载，只有显示成功后才休眠 | 初始化完成立即 hibernate，首次下载失败也保持休眠 |

已有的图片、英文新闻、中文新闻刷新路径均调用 hibernate，继续保留。实际驱动为 GxEPD2_426_GDEQ0426T82 / SSD1677，800×480，RST 接 GPIO8；驱动下一次绘制时会复位并唤醒屏幕。begin() 的成功只表示软件初始化完成，不能证明屏幕接线或 BUSY 响应正常。

正常轮播：图片每页 10 分钟，英文新闻每页 5 分钟，中文新闻每页 5 分钟；一轮共 70 分钟，另加屏幕刷新耗时；内容正常每 2 小时下载一次，00:00–08:00 禁网期间顺延。正常运行且传感器关闭、校时成功时，通常一次睡眠接近当前页面剩余显示时长。应用任务保留原重试间隔，但 Wi-Fi 连接失败额外执行 1、2、4、8、15 分钟的退避，上限 15 分钟；退避期间任务返回失败而不开无线，连接成功后重置。休眠调度已纳入退避截止时间；代价是路由器恢复后最长可能等待约 15 分钟再尝试连接（另计当时正在执行的任务耗时）。

采用浅睡眠以保留 PSRAM 中的图片、新闻和程序状态。不要直接改为 esp_deep_sleep_start()，它会重新启动程序并丢失当前缓存，需要先实现持久化缓存和恢复机制。本项目没有启动蓝牙，因此无需额外初始化蓝牙再关闭。现有预编译 SDK 未启用 CONFIG_PM_ENABLE，因此单纯 delay() 无法替代显式浅睡眠。

## 可配置项

- AppConfig::kLightSleepEnabled：默认 true。调试连续 USB 串口时可改 false；Wi-Fi 仍按需关闭。原生 USB 在浅睡眠期间可能暂时不可用，连接电脑测量也可能影响结果。
- AppConfig::kInitialPreviewEnabled：默认 true，保留首次快速预览。电池使用可改 false，避免启动时快速刷新十个页面再回首页。
- kImagePageDisplayDurationMs、kNewsPageDisplayDurationMs、kChineseNewsPageDisplayDurationMs：分别独立设置图片、英文新闻、中文新闻每页显示时长。
- SHT41 默认关闭；启用后保留 30 秒采样周期，会缩短每次睡眠时间。传感器采用单次测量，无加热指令。

未调整 CPU 频率、Wi-Fi 发射功率、屏幕刷新波形或未知电源 GPIO；这些需要性能、信号或硬件依据。休眠失败会打印错误并退避 1 秒，便于发现设备实际上未进入低功耗状态。

## 验证与实测

软件验证使用现有构建参数（ESP32-S3、240 MHz、4 MB Flash、huge_app、PSRAM enabled、USB HW CDC）。定时测试：

```sh
c++ -std=c++11 -Wall -Wextra -Werror -I. tests/sleep_schedule_test.cpp -o /tmp/sketch_aug27a-sleep-test
/tmp/sketch_aug27a-sleep-test
```

测试覆盖到期、超期、约 49.7 天 millis() 回绕，以及两小时刷新与失败重试同时满足的条件。编译和主机测试不能验证实际唤醒、屏幕状态和电流；当前没有识别到连接的 ESP32，未烧录。

建议上板依次检查：

1. 启动下载、快速预览、所有页面轮播，确认浅睡眠后图片缓存完好且能刷新屏幕。
2. 跨过两小时更新，确认重新联网、下载后再次进入低电流平台。
3. 临时关闭路由器，观察重试期间能否休眠；恢复网络后确认自动恢复下载。
4. 用同一供电条件比较修改前后电流，分别记录下载、屏幕刷新和静态显示，积分覆盖完整轮播周期。单个瞬时读数不能代表平均功耗。

整板待机还受稳压器静态电流、板载 RGB 灯、外接电源模块和墨水屏转接板影响。屏幕 hibernate 不等于整个转接板断电。如有受控电源开关，还需处理 SPI/GPIO 反向供电。

## ESP32-S3-Zero 板卡资料核对

根据用户提供的《ESP32-S3-Zero - Waveshare Wiki.pdf》第 1、2、6、16 页及其链接的[官方原理图](https://www.waveshare.net/w/upload/f/fc/ESP32-S3-Zero-Sch.pdf)：

- ESP32-S3FH4R2，4 MB Flash、2 MB PSRAM；当前 4 MB Flash / QSPI PSRAM 构建配置匹配。2 MB 是整片 PSRAM 容量；应用的单张图片 2 MB 上限并不意味着四张图可各占 2 MB，实际分配不足会返回失败。
- 板载 WS2812B-0807 的 DIN 接 GPIO21，R1 = 1 kOhm 上拉到 3.3 V。新增启动时发送全零熄灯帧、拉低 1 ms 锁存、释放 RMT 并最终保持 GPIO21 高电平的处理。持续拉低会在 R1 上产生约 3.3 V / 1 kOhm = 3.3 mA 电流；这是电路计算，不是本机实测，也不是相对于原固件必然节省的数值（原代码未主动拉低该引脚）。RGB 熄灭后其控制芯片仍供电，静态电流不能通过熄灯命令完全消除。换板时应核对电路，或将 kBoardRgbLedPin 设为 -1。
- 官方原理图显示稳压器为 ME6217C33M5G，EN 接 VBUS；RGB 直接接 3V3，没有独立的 GPIO 电源开关。因此不能用软件单独切断 RGB 的电源，也不能通过关闭此稳压器维持 MCU 的正常休眠唤醒。实际器件版本仍以实物为准。
- 此板没有独立 USB 转 UART 芯片，前述通用排查中的 USB 转串口耗电不适用于本板。USB 数据线仍直接接 MCU，需要考虑原生 USB 在休眠中的行为。
- Wiki 的“5 V 供电最少 500 mA”是电源供电能力要求，不能解读为板卡持续消耗 500 mA。原理图中 USB VBUS 与 5 V 排针同网，不应把它们当作相互隔离的两路输入。
- 现有屏幕 GPIO7～12 和传感器 GPIO5/6 未与板载 RGB GPIO21、USB GPIO19/20 冲突。

供电已确认：三节 2.55 Ah 的 18650 并联，经 DC-DC 升压模块供电，标称总容量 7.65 Ah。升压模块型号、输出电压、静态电流及低负载效率尚未确认；墨水屏转接板型号也未提供。下一步可在断电拆接后，分别测量仅主板和连接墨水屏时的静态电流，定位外设增量；不要把芯片数据手册中的休眠电流直接当成整板指标。电池端测量可直接计入 DC-DC 自身损耗；之前按 85% 效率给出的续航表只是条件估算。

## 整体走查：缓存、网络与异常路径

本轮追加修改：

1. **缓存显示帧**：下载 PNG 后立即校验、解码、旋转为 800×480 单色帧；每张 48,000 字节，四张常驻 192,000 字节。只有解码成功且输出行数完整才 swap 替换旧帧，失败继续保留旧图并等待重新下载。PNG_CHECK_CRC 开启；此前只检查签名、打印无参考值的 FNV 校验值，无法证明图片可解码。原 ImageInspector 保留为诊断工具，主流程不再依赖它。
2. **翻页免解码**：直接发送缓存帧。仅启用 SHT41 时，天气传感器叠层使用单独的 48,000 字节工作帧，避免污染缓存；临时 PNG 在当前下载循环结束时释放。192,000 字节仅为四张图缓存，不含新闻、工作帧、解码器及下载临时区，也不代表比任意高度压缩的 PNG 集合更小。主要收益是缓存容量固定及避免重复解码。
3. **有界响应接收**：登录最多 8 KiB、新闻最多 32 KiB，在写入 PSRAM 时检查长度；没有 Content-Length 的响应也受限制。HTTPClient 继续处理 chunked 编码，完整接收后才解析 JSON。接收失败、长度不一致或超限时保留旧新闻。限制针对响应正文，不是对 HTTPClient 内部所有头部解析或整次请求总时长的保证。
4. **网络耗电**：连接失败指数退避；启动 NTP 与内容下载共用连接，嵌套 WifiSession 只在最外层退出时关闭无线。HTTP TCP 连接超时设为 10 秒、TLS 握手 15 秒、读超时 15 秒；读超时是等待数据的超时，不是整次下载的硬截止时间。
5. **边界保护**：新闻写入同时受实际数组容量约束；流写入处理零长度、容量超限及 size_t 溢出；校时失败重试从操作结束计时。

验证：原 ESP32-S3 配置编译通过，固件 1,456,207 字节，静态 RAM 103,260 字节（不含动态 PSRAM）。定时测试及下列生产缓冲区/退避代码测试通过；主机测试开启 AddressSanitizer、UndefinedBehaviorSanitizer，覆盖内存分配失败不破坏已有数据、超限/溢出拒绝、缓存交换、退避封顶/复位及时间回绕。

```sh
c++ -std=c++11 -Wall -Wextra -Werror -fsanitize=address,undefined -Itests/support -I. tests/power_robustness_test.cpp PsramImage.cpp -o /tmp/sketch_aug27a-power-test
/tmp/sketch_aug27a-power-test
```

剩余边界：尚未上板验证 CRC 开启后的实际服务端图片、浅睡眠后的显示与两小时更新；首次启动已支持展示成功获取的部分内容，缺失页面跳过并继续重试；HTTPS 仍沿用原项目的 setInsecure()，尚未配置服务端可信 CA，不能视作已完成服务端身份校验。传感器默认关闭；其读数 CRC、单次测量与重试路径已检查。未配置电池电压采样分压电路，因此没有加入未经硬件支持的低电量判定。

参考：[Espressif ESP32-S3 睡眠模式](https://docs.espressif.com/projects/esp-idf/en/stable/esp32s3/api-reference/system/sleep_modes.html)。实际实现另外核对了本机 Arduino millis() 使用 esp_timer_get_time()、WiFi.mode(WIFI_OFF) 停止无线及 GxEPD2 hibernate 的复位恢复路径。


## 追加优化：部分内容启动与休眠调度

- 本轮下载结束后，只要有可用图片或新闻页就开始轮播，单个接口失败不再阻止全部显示。没有任何可用内容时保留屏幕并按重试计划等待。
- 新闻条数不足以填满所有分页时只展示有内容的页面；已下载图片以缓存有效性判断。快速预览按页序向后查找，不循环等待缺失页；正常轮播循环查找下一可用页。完整数据的一轮仍约 70 分钟，部分数据时只轮播可用页。
- 部分启动后，待获取内容按 60 秒间隔重试，并受 Wi-Fi 退避约束；所有内容更新成功后重新开始两小时更新计时。已有成功内容不随每次失败重试重复下载。
- 内容请求与浅睡眠共用 contentWaitMs()，统一处理正常更新、缺失内容重试和 Wi-Fi 退避。NTP 和启动重试也纳入无线退避，避免退避期间每分钟无效唤醒；翻页或传感器到期仍正常唤醒。
- 新增页选择与调度回归测试：无可用页面、缺页跳转、末页回绕、单页、非回绕预览结束、部分数据立即进入重试计划、长退避及 millis() 回绕。主机测试启用地址与未定义行为检查并通过。

未改变正常内容更新周期（2 小时）、图片页时长（10 分钟）、中英文新闻页时长（5 分钟）或首次预览时长（10 秒）。仍需实际设备验证部分接口失败后的轮播和离线恢复。

本次编译通过：程序 1,456,623 字节（3 MiB 程序分区的 46%），静态 RAM 103,252 字节（31%），剩余 224,428 字节供堆栈等运行时使用。尚未烧录。


## 夜间禁网：北京时间 00:00–08:00

- 使用 CST-8（北京时间 / UTC+8），每天 00:00:00 起禁止联网，08:00:00 起允许处理到期请求。覆盖 Wi-Fi 连接、NTP、登录、图片、新闻及失败重试。夜间仍按原时长使用缓存翻页。
- 夜间到期的两小时更新顺延，早上恢复后只执行一次待处理更新，不补发整个夜间每个周期的请求；所有内容成功后重新计时两小时。
- 为阻止跨午夜的阻塞请求，独立 FreeRTOS 任务在 CPU 醒着时每 250 ms 检查时间并调用 esp_wifi_stop()，失败会继续尝试；正常请求入口也检查时段。关停存在任务调度和驱动执行延迟，不是硬实时零延迟保证。浅睡眠时无线已关闭，监控任务暂停，不为轮询额外唤醒芯片。
- 用户已允许完全断电重启后的**一次启动联网校时**。仅在系统没有有效时间时开放这次例外；先校时再请求应用数据。校时成功若在夜间，立即禁网，直到 08:00 才获取内容。断电后 PSRAM 缓存丢失，夜间冷启动不能下载新页面，墨水屏保留原画面。
- 如果这一次校时失败且时间仍无效，保持禁网，需恢复网络后重启再尝试；不会在不知道当前时段的情况下无限重试校时。监控任务创建失败也禁止联网。
- 测试覆盖 00:00:00、00:00:01、07:59:59、08:00:00、白天及跨午夜前一秒，并验证禁网时延后重试、08:00 恢复到期请求。实际跨午夜中断、早晨恢复及校时失败路径仍需上板验证。

新增监控任务栈为 3,072 字节，来自运行时内存，未包含在编译器的静态 RAM 报告中。规则按设备系统时间判断，实际时钟误差会影响禁网边界。


## 凌晨翻页时长

北京时间 02:00:00（含）至 08:00:00（不含），所有可用图片和新闻页每页显示 30 分钟；此规则也覆盖首次预览。其他时间恢复图片每页 10 分钟、英文/中文新闻每页 5 分钟，首次预览常规为每页 10 秒。失败重试间隔保持原值。

调度会在 02:00 和 08:00 唤醒并重新计算当前页的目标时长，以该页上次成功显示时间为起点。08:00 恢复后，如果当前页已经超过常规时长，会在处理到期联网任务后翻页；不会继续等待剩余的半小时。原 00:00–08:00 禁网规则不变。

回归测试覆盖 01:59:59、02:00:00、07:59:59、08:00:00、次日边界及图片/新闻/预览的时长覆盖。实际时段切换仍需上板验证。


## 最新全面检查：SHT41 保留且关闭

- `kSht41Enabled = false`；Sht41Sensor.cpp/.h 原样保留。主流程不会初始化 I2C、读取传感器、安排传感器定时唤醒。
- 修正天气页显示入口：只有传感器启用时才绘制温湿度叠层。关闭时直接显示缓存图，不再覆盖温湿度占位框，也不再触发额外 48,000 字节工作帧分配和每次拷贝。相关绘制代码仍保留。
- 修正 Wi-Fi 关闭失败路径：只有驱动确认关闭成功才清除 radioActive，失败时保留追踪，使夜间监控仍可继续关停无线。
- 修正预览/正常轮播的空缓存路径：清除轮播状态、重试标记并进入定时休眠，防止到期后不断执行空的翻页检查。
- 更新预览日志，使其包含 02:00–08:00 的 30 分钟时长规则。

验证结果：

1. 定时、禁网、翻页时段、缺页选择测试通过。
2. 生产 PSRAM 缓冲区的容量、溢出、分配失败、交换及重连退避测试通过。
3. 新增测试直接编译执行真实 sketch 的 setup()/loop()，仅替换硬件/服务边界，验证部分下载可启动、跳过缺页、02:00/08:00 时长切换、两种轮播模式的空缓存休眠，以及 SHT41 初始化/采样次数始终为零、天气页不请求传感器叠层。模拟测试不验证真实 Wi-Fi 驱动、屏幕或传感器电气行为。

三组主机测试均启用 AddressSanitizer 和 UndefinedBehaviorSanitizer，测试期间未报告错误。主流程测试命令：

```sh
c++ -std=c++17 -Wall -Wextra -Werror -fsanitize=address,undefined -Itests/support -I. tests/sketch_runtime_test.cpp PsramImage.cpp -o /tmp/sketch_aug27a-runtime-test
/tmp/sketch_aug27a-runtime-test
```

当前完整编译通过：程序 1,457,935 字节（46%），静态 RAM 103,260 字节（31%），静态分配后剩余 224,420 字节。运行时任务栈、TLS、新闻和 PSRAM 缓存另计。未检测到连接的 ESP32，未烧录或测量电流。

最终保留配置：每两小时获取内容，00:00–08:00 禁网（已授权的一次冷启动校时除外）；02:00–08:00 所有页每页 30 分钟，其余正常轮播图片每页 10 分钟、新闻每页 5 分钟。
